# real-time kernal 5.15
## reference: [5.15 install tutorial from CSDN](https://blog.csdn.net/lalahang/article/details/143725771?ops_request_misc=%257B%2522request%255Fid%2522%253A%25224623008a296eef3062424733f918b3d0%2522%252C%2522scm%2522%253A%252220140713.130102334.pc%255Fall.%2522%257D&request_id=4623008a296eef3062424733f918b3d0&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~first_rank_ecpm_v1~rank_v31_ecpm-3-143725771-null-null.142%5ev102%5econtrol&utm_term=%E5%AE%9E%E6%97%B6%E5%86%85%E6%A0%B8%E5%AE%89%E8%A3%85&spm=1018.2226.3001.4187)

## 1. 下载安装包
```
curl -SLO https://www.kernel.org/pub/linux/kernel/v5.x/linux-5.15.119.tar.gz
curl -SLO https://www.kernel.org/pub/linux/kernel/projects/rt/5.15/older/patch-5.15.119-rt65.patch.gz
```
## 2. 安装必要的依赖
```
sudo apt-get install build-essential bc curl ca-certificates gnupg2 libssl-dev lsb-release libelf-dev bison flex dwarves zstd libncurses-dev
```
## 3. 解压安装包和补丁
```
tar xvzf linux-5.15.119.tar.gz
gunzip patch-5.15.119-rt65.patch.gz
```
## 4. 提取源代码并应用补丁
```
cd linux-5.15.119
patch -p1 < ../patch-5.15.119-rt65.patch
```
## 5. 使用当前核的配置作为实时核的默认配置
```
make olddefconfig
```
## 6. 按照实时内核的配置修改配置文件
```
make menuconfig
```
这个命令会打开一个终端界面，你可以在其中配置抢占模型。使用箭头键导航到 `General Setup > Preemption Model` 并选择 `Fully Preemptible Kernel (Real-Time)` 。
之后导航到 `Cryptographic API > Certificates for signature checking （在列表的最底部）> Provide system-wide ring of trusted keys > Additional X.509 keys for default system keyring`。从提示符中移除 `“debian/canonical-certs.pem”`，然后按`OK`。将此配置保存到 `.config` 并退出 `TUI`。其他选项保留默认值。

## 7. 打开并编辑文档进一步编辑
```
gedit .config
```
将如下指令
```
    CONFIG_SYSTEM_TRUSTED_KEYS="debian/canonical-certs.pem"
    CONFIG_SYSTEM_REVOCATION_KEYS="debian/canonical-revoked-certs.pem"
    CONFIG_DEBUG_INFO_BTF=y
```
替换成
```
    CONFIG_SYSTEM_TRUSTED_KEYS=""
    CONFIG_SYSTEM_REVOCATION_KEYS=""
    CONFIG_DEBUG_INFO_BTF=n
```
 为了防止编译过程保存`log`信息占用大量磁盘空间，注释: `CONFIG_DEBUG_INFO=y`
## 8. 开始编译内核
```
fakeroot make -j24 deb-pkg
```
 